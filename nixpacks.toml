[phases.setup]
aptPkgs = ["chromium-browser", "fonts-freefont-ttf", "libatk-bridge2.0-0", "libgtk-3-0", "libnss3", "libxss1", "libasound2"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "/usr/bin/chromium-browser"
